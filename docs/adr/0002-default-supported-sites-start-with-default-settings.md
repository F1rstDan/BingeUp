# Default-supported sites start with default settings

Bilibili 与 YouTube 作为默认支持网站，在安装时取得运行所需的主机权限并以默认设置启用；安装引导只是让用户关闭或重新开启这些网站的可选配置入口，不构成正式学习的启动门禁，关闭或跳过引导会保留默认配置并允许正式学习启动。该选择优先保证 Beta 安装后立即可用、便于验证核心学习闭环；用户主动添加的其他网站仍使用按需可选权限。
